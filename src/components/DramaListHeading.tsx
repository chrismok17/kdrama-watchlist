import React from "react";

const DramaListHeading = (props: any) => {
    return (
        <div className="col">
            <h1>{props.heading}</h1>
        </div>
    );
};

export default DramaListHeading;